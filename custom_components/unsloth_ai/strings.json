{
  "config": {
    "step": {
      "user": {
        "title": "Connect to your Unsloth model server",
        "description": "Point this at the OpenAI-compatible base URL of your inference server (ending in /v1).",
        "data": {
          "api_url": "API base URL",
          "model_name": "Model name",
          "api_key": "API key (optional)"
        }
      }
    },
    "error": {
      "invalid_url": "URL must start with http:// or https://",
      "cannot_connect": "Could not connect to the model server",
      "invalid_auth": "The API key was rejected"
    },
    "abort": {
      "already_configured": "This server is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Model options",
        "data": {
          "prompt": "Instructions (system prompt template)",
          "llm_hass_api": "Control Home Assistant (LLM API)",
          "temperature": "Temperature",
          "max_tokens": "Max tokens",
          "timeout": "Request timeout (seconds)",
          "think": "Thinking (reasoning mode) \u2014 off is faster and avoids empty replies",
          "max_history": "Max history (previous turns sent to the model, 0 = unlimited)"
        },
        "description": "Select 'Assist' under Control Home Assistant to let the model see and control your devices."
      }
    }
  }
}